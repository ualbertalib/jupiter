<% page_title(@collection.title) %>

<h1 class="mt-3">
    <%= t('.header', title: @collection.title) %>
</h1>

<% if policy(@collection).edit? %>
  <div class="d-flex justify-content-between mt-3">
    <h5>
      <%= link_to t('.parent_community', title: @community.title), community_path(@community) %>
    </h5>
    <%= link_to t('edit'),
                edit_community_collection_path(@community, @collection),
                class:'btn btn-outline-primary' %>
  </div>
<% end %>

<h5 class="items-header">
  <%= t('.items_list_header') %>
</h5>

<div class="row mt-3">
  <div class="col">
    <div class="list-group">
      <% if @collection.member_items.count > 0 %>
        <% @collection.member_items.each do |item| %>
          <%= link_to item.title, item_path(item), class: 'list-group-item list-group-item-action' %>
        <% end %>
      <% else %>
          <%= t('.no_items_in_collection') %>
      <% end %>
    </div>
  </div>
</div>
