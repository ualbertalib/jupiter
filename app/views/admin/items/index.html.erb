<% page_title(t('.header')) %>

<div class="d-flex flex-wrap align-items-center justify-content-between my-3">
  <h1>
    <%= t('.header') %>
  </h1>
  <div>
    <%= link_to create_draft_items_path, method: :post,
        class: 'btn btn-primary' do %>
      <%= fa_icon('plus-circle') %>
      <%= t('items.new.header') %>
    <% end %>

    <%= link_to '#TODO', class: 'btn btn-primary' do %>
      <%= fa_icon('plus-circle') %>
      (TODO) <%= t('.deposit_thesis') %>
    <% end %>
  </div>
</div>

<%= render partial: 'search/item_search_and_results',
          locals: { search_placeholder: t('.search_placeholder'),
                    no_items_found: t('.no_items_found') }%>
