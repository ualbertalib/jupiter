<h1>Batch Ingests</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @batch_ingests.each do |batch_ingest| %>
      <tr>
        <td><%= link_to batch_ingest.title, admin_batch_ingest_path(batch_ingest) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Batch Ingest', new_admin_batch_ingest_path %>
