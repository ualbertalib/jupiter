<h2 class="mt-3">
  <%= "Work: #{@work.title}" %>
</h2>
<div class="row mt-3">
  <div class="col">
    <div class="list-group">
      <% @work.display_attributes.each do |name, value| %>
        <div class="list-group-item">
          <%= "#{name.titlecase} : #{value}" %>
        </div>
      <% end %>
      <%# probably remove this if check below, just added to prevent this from 500ing %>
      <% if @work.member_of_paths %>
        <div class="list-group-item flex-column">
          Member of:
          <% @work.member_of_paths.each do |path| %>
            <div class="d-flex">
              <%= to_named_path(path) %>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>
<div class="row mt-3">
  <div class="col">
    <h4>Contains files:</h4>
    <table class="table">
      <thead>
        <tr>
          <th>FileSet</th>
          <th>Original Filename</th>
          <th>MIME Type</th>
        </tr>
      </thead>
      <tbody></tbody>
    </table>
  </div>
</div>
<% # 				- @work.file_sets.each_with_index do |fs,idx| %>
<% # 					- fs.files.each do |file| %>
<% # 						%tr %>
<% # 							%td=idx + 1 %>
<% # 							%td=file.original_name %>
<% # 							%td=file.mime_type %>
<% # .row.mt-3 %>

<div class="col">
  <%= link_to 'Edit', edit_work_path(@work), class: 'btn btn-primary pull-right' %>
</div>
