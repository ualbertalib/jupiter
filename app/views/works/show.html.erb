<% page_title( t('.header', title: @work.title)) %>

<%= render partial: 'breadcrumbs' %>

<h1 class="mt-3">
  <%= t('.header', title: @work.title) %>
</h1>
<div class="row mt-3">
  <div class="col">
    <div class="list-group">
      <% @work.display_attributes.each do |name, value| %>
        <div class="list-group-item">
          <%= "#{name.titlecase} : #{value}" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
<div class="row mt-3">
  <div class="col">
    <h4><%= t('.contain_files') %></h4>
    <table class="table">
      <thead>
        <tr>
          <th><%= t('.fileset') %></th>
          <th><%= t('.original_filename') %></th>
          <th><%= t('.mime_type') %></th>
        </tr>
      </thead>
      <tbody></tbody>
    </table>
  </div>
</div>
<% # 				- @work.file_sets.each_with_index do |fs,idx| %>
<% # 					- fs.files.each do |file| %>
<% # 						%tr %>
<% # 							%td=idx + 1 %>
<% # 							%td=file.original_name %>
<% # 							%td=file.mime_type %>
<% # .row.mt-3 %>

<div class="col">
  <%= link_to t('edit'), edit_work_path(@work), class: 'btn btn-primary pull-right' %>
</div>
